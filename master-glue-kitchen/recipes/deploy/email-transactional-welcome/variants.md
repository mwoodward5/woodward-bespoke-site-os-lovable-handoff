# Variants — Email Transactional Welcome

- **Intensity:** low / medium / high
- **Palette:** override via `tokens/palette-*.json` in fx-kitchen
- **Typography:** override via `tokens/type-pair-*.json` in this kitchen
- **Locale:** pass `lang` in props; templates re-shape for RTL when needed
- **Density:** scale spacing/type-scale from `tokens/scale-*.json`
